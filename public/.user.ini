; PHP settings for file uploads (10MB limit + buffer)
; Used by PHP-FPM / CGI (many shared hosts and VPS)
; Takes effect after PHP restarts or after the configured request interval (often ~5 min)

upload_max_filesize = 12M
post_max_size = 12M
memory_limit = 256M
max_execution_time = 120
